### NOT CURRENTLY USED
databaseChangeLog:
  - changeset:
      id: createdb-sampledata1-postgres
      author: biporigin
#      runOnChange: false
      runInTransaction: false
      changes:
        - sql:
            sql: SELECT 'CREATE SCHEMA sampledata1' WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'sampledata1')\gexec
      